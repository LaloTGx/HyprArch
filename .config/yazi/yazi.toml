[opener]
edit = [
    { run = 'nvim "$@"', block = true, for = "unix" }
]

[open]
rules = [
    { name = "*.txt", use = "edit" },
    { mime = "text/*", use = "edit" },
    { mime = "application/json", use = "edit" },
    { mime = "inode/x-empty", use = "edit" }
]
